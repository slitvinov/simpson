procedure kuncir(a, b, f, I, i, eps, N);
value a, b, eps, N;
integer N;
real a, b, I, i, eps;
real procedure f;
code;

real procedure fun(x);
value x;
real x;
begin
  print(x);
  fun := sin(x*x)
end;

begin
  real a, b, I, eps;
  integer i, N;
  a := 0.0;
  b := 3.0;
  eps := 0.001;
  N := 100;
  kuncir(a, b, fun, I, i, eps, N);
  outreal(1, I);
  outstring(1, "\n")
end
